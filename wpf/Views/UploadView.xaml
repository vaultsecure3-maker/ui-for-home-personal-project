<UserControl x:Class="DocumentManagement.Views.UploadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{StaticResource Gray50Brush}">

    <Grid RowDefinitions="Auto,*">
        <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource Gray200Brush}" BorderThickness="0,0,0,1"
                Padding="32,24,32,24">
            <StackPanel>
                <TextBlock Text="Upload" Style="{StaticResource TitleLargeStyle}" />
                <TextBlock Text="Add new documents to your archive" Style="{StaticResource BodySmallStyle}"
                          Margin="0,4,0,0" Foreground="{StaticResource Gray600Brush}" />
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" Padding="32">
            <StackPanel Spacing="24">
                <!-- Drop Zone -->
                <Border Background="White" BorderBrush="{StaticResource Gray200Brush}" BorderThickness="2"
                       BorderDashArray="5,5" Padding="32" CornerRadius="4">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="16">
                        <materialDesign:PackIcon Kind="CloudUpload" Width="48" Height="48"
                                                Foreground="{StaticResource Gray400Brush}"
                                                HorizontalAlignment="Center" />
                        <TextBlock Text="Drag files here or click to browse" Style="{StaticResource BodySmallStyle}"
                                 Foreground="{StaticResource Gray600Brush}" HorizontalAlignment="Center" />
                    </StackPanel>
                </Border>

                <!-- Metadata Form -->
                <StackPanel Spacing="12">
                    <TextBlock Text="Default Metadata" Style="{StaticResource TitleMediumStyle}" />
                    <StackPanel Spacing="12">
                        <StackPanel Spacing="4">
                            <TextBlock Text="Correspondent" Style="{StaticResource CaptionStyle}" />
                            <ComboBox Padding="8,6,8,6" />
                        </StackPanel>
                        <StackPanel Spacing="4">
                            <TextBlock Text="Document Type" Style="{StaticResource CaptionStyle}" />
                            <ComboBox Padding="8,6,8,6" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <!-- Upload Button -->
                <Button Style="{StaticResource PrimaryButtonStyle}" Height="40" Padding="24,0,24,0">
                    <TextBlock Text="Upload Files" />
                </Button>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
